// ── Loader Injected Logic (แจกลูกค้า) ──

// ไฟล์นี้มักจะว่างเปล่าในตอนเริ่มต้น
// และจะถูกฉีด (Inject) โดย core_content.js ที่โหลดมาจาก server อีกทีหนึ่ง

console.log('[Arin Loader] Injected stub active.');